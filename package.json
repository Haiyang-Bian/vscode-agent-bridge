{
  "name": "vscode-agent-bridge",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun scripts/run-workspaces.ts build",
    "typecheck": "bun scripts/run-workspaces.ts typecheck",
    "test": "bun test",
    "check": "bun scripts/check.ts"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
